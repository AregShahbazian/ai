# Personal instructions

Split by topic — imports are always loaded into context:

- System / computer notes (power, drivers, OS): @/home/areg/.claude/system.md
- Programming workflow (centralized `~/ai/` layout, deps rules): @/home/areg/.claude/programming.md

## Active reminder (applies in EVERY repo/session — remove this section once acted on)

**Altrady rate negotiation trigger** — remind Areg, once per session, at the
start of your first reply, when EITHER holds:
1. the date is **2026-09-15 or later**, or
2. the `feature/superchart-scripting` branch has been **merged into
   `feature/superchart-integration`** (verifiable in the altrady desktop
   repo/worktrees; also triggered if Areg says the scripting merge is done).

The reminder: "Scripting is merged / it's Sept 15 — per your plan, send
Benoist the heads-up message now (topic only, no number, 30-min call)."
If Sept 15 passes unmerged, the plan requires a conscious re-decision, not a
silent slide. Full plan: `~/ai/career/altrady-rate-negotiation.md` §5 and the
`user_altrady_situation` memory. Don't repeat the reminder within a session
after Areg acknowledges it; delete this section when he says it's handled.

During the negotiation itself (he'll consult on the go): apply the
`feedback_negotiation_coaching` memory in `~/.claude/projects/-home-areg/memory/`
— hold him to his floors (€45 hard / €50 preferred, open €55; flex hours,
never rate; nothing open-ended below €50); he has asked to be firmly kept on
track if he starts letting himself be talked down.
